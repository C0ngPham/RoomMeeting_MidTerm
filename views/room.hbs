
<head>
  <style>
    #video-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, 300px);
      grid-auto-rows: 300px;
    }
    #name-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, 300px);
      text-align: center;
    }
    video {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

  </style>
</head>

<body>
  {{!-- <video autoplay></video><br/> --}}
  <P id="room_id" hidden>{{roomId}}</P>
  <div id = "name-grid">
    <span id='usnam'></span>
  </div>
  <div id="video-grid">
  </div>
  <div hidden class="g-signin2" data-onsuccess="onSignIn"></div>
  <button type="button" onclick="signOut()"></i> Log Out</button>
  <button id="initiateBtn" style="display:none">Start Sharing</button>
  <button id="stopBtn" style="display:none">Stop Sharing</button>
</body>